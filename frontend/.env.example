# pslog frontend 환경변수 템플릿 — `make setup` 이 .env.local 자동 생성.
# Vite 는 .env.local 만 읽음 (gitignore).

# Backend API base URL — Makefile 기본값: 8081 (app-chak 8000 과 충돌 회피)
VITE_API_URL=http://localhost:8081/api/v1
